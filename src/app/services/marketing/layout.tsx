export const metadata = {
  title: "Top-rated Social Media Marketing Services | Bytes Platform",
  description: "Transform your social presence into conversion with Bytes Platform, creating social media strategies, content, and campaigns that boost brand awareness.",
  alternates: {
    canonical: "/services/marketing",
  },
};

export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 